{
  "name": "zerodha-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "build": "cd frontend && npm run build -- --no-sourcemap"
  }
}
